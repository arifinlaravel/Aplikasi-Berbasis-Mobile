import 'package:flutter/material.dart';

class StaffTicketValidationPage extends StatelessWidget {
  const StaffTicketValidationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Validasi Tiket')),
      body: const Center(child: Text('Halaman validasi tiket (contoh)')),
    );
  }
}
